# Claude Code fallback

The actual assistant instructions live in `AGENTS.md`. This file exists only
because Claude Code looks for `CLAUDE.md` specifically and doesn't read
`AGENTS.md` on its own — read `AGENTS.md` now and follow it.
